# TODO: вопросы и решения по ходу работы

Рабочий журнал. Вопросы и замечания дописывать пунктами снизу, ответ в строке `**Ответ:**`.
Решённые пункты не удаляем — помечаем ✅ и переносим смысл решения в `PLAN.md` / код.

## Открытые вопросы

1. **Судьба модели `Review`.** В плане предложено удалить её, а `rating` + `text` перенести полями
   в `ReadingEntry` (одна запись = книга + статус + моя оценка). Альтернатива — оставить `Review`
   отдельной моделью, а `ReadingEntry` сделать чисто статусной. Не ломает ли удаление `Review`
   сданные этапы курса (`stage-1`…`stage-3`)?
   **Ответ:**

2. **Набор статусов.** Достаточно трёх (`planned` / `reading` / `read`) или нужен ещё
   «брошено» (`abandoned`)? Он часто нужен, чтобы книга не висела вечно в «Читаю».
   **Ответ:**

3. **Повторное чтение.** `unique_together (reader, book)` запрещает перечитывание — вторую запись
   о той же книге завести нельзя. Оставляем такое ограничение или нужна история прочтений?
   **Ответ:**

4. **Даты `started_at` / `finished_at`.** Проставлять автоматически при смене статуса, или дать
   пользователю править их руками (например, внести книгу, прочитанную в прошлом году)?
   **Ответ:**

5. **Книга только из каталога?** Чтобы добавить книгу в дневник, её сначала нужно завести в общем
   каталоге (форма с автором и жанрами). Это заметный барьер. Делать ли быстрое добавление
   «название + автор строкой» прямо со страницы дневника?
   **Ответ:**

6. **Приватность дневника.** Публичная страница читателя (`/auth/users/<pk>/`) сейчас показывает
   всё. Дневник там показываем целиком, только прочитанное, или прячем совсем?
   **Ответ:**

7. **`Book.added_by` с `on_delete=CASCADE`.** Удаление пользователя удалит из общего каталога все
   добавленные им книги вместе с чужими записями дневника. Похоже на ошибку — поменять на
   `SET_NULL` (поле станет nullable)? Данных нет, миграцию всё равно пересобираем.
   **Ответ:**

8. **Заметки и прогресс.** Нужны ли по ходу чтения поля «заметки/цитаты» и прогресс
   (страница или %), или для учебного проекта это лишнее?
   **Ответ:**

9. **Фоновая задача.** Сейчас Celery логирует добавление книги в каталог. Перевесить задачу на
   события дневника (например, «книга дочитана» — лог/уведомление), или оставить как есть?
   **Ответ:**

## Решённые

- ✅ Проект не развёрнут, боевых данных нет: миграции пересобираем с нуля, `db.sqlite3` и
  `fixtures/bookshelf_export.json` выбрасываем.
